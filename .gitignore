# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated data is committed (per spec 02) so the app runs without a build step.
# Rebuild with `npm run transform` whenever the source JSON changes.

# V8 coverage reports — regenerated by `npm run test:coverage`. The derived
# public/coverage-badge.svg IS committed so the README renders on a fresh clone.
coverage/
